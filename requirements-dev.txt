# Development dependencies for local testing
# Install this for local development only, not for Databricks
# Usage: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
